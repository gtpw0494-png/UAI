# Contributing

IntraultUniversalion uses additive, evidence-backed development. Changes must preserve OneChat, capability truth, provenance, rollback, and the project doctrine record. Do not mark a capability CONNECTED without executable evidence. Run `npm test` before proposing changes. Neural-only tests are optional unless the change touches ForgeLM.
