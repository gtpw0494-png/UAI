import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';

const RISK=new Set(['low','medium','high','critical']);
const canon=v=>Array.isArray(v)?`[${v.map(canon).join(',')}]`:v&&typeof v==='object'?`{${Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+canon(v[k])).join(',')}}`:JSON.stringify(v);
export function pluginManifestDigest(manifest){const clean={...manifest};delete clean.signature;return crypto.createHash('sha256').update(canon(clean)).digest('hex');}
export function validatePluginManifest(m={}){
  const errors=[];if(!/^[a-z0-9][a-z0-9._-]{1,127}$/.test(String(m.id||'')))errors.push('invalid id');if(!m.name)errors.push('name required');if(!m.version)errors.push('version required');if(!m.entrypoint)errors.push('entrypoint required');if(!Array.isArray(m.capabilities))errors.push('capabilities must be an array');if(!RISK.has(String(m.risk)))errors.push('invalid risk');
  const p=m.permissions||{};if(!Array.isArray(p.filesystem?.read)||!Array.isArray(p.filesystem?.write))errors.push('filesystem read/write arrays required');if(!Array.isArray(p.network?.allow))errors.push('network allow array required');if(typeof p.process?.spawn!=='boolean')errors.push('process.spawn boolean required');const t=Number(m.timeoutMs);if(!Number.isInteger(t)||t<100||t>300000)errors.push('timeoutMs out of range');if(!/^[a-f0-9]{64}$/i.test(String(m.provenance?.sha256||'')))errors.push('provenance.sha256 required');return {state:errors.length?'BLOCKED':'SUCCESS',errors};
}
export function verifyPluginSignature(m={}){if(!m.signature)return {state:'UNAVAILABLE',verified:false,reason:'No signature supplied.'};if(m.signature.algorithm!=='ed25519')return {state:'BLOCKED',verified:false,reason:'Only ed25519 signatures are supported.'};try{const ok=crypto.verify(null,Buffer.from(pluginManifestDigest(m),'utf8'),m.signature.publicKeyPem,Buffer.from(m.signature.signatureBase64,'base64'));return {state:ok?'SUCCESS':'DENIED',verified:ok,reason:ok?'Manifest signature verified.':'Manifest signature verification failed.'};}catch(e){return {state:'ERROR',verified:false,reason:e.message};}}
export class PluginRegistry{
 constructor(stateRoot,audit=null){this.file=path.join(stateRoot,'plugins-v1.json');this.audit=audit;fs.mkdirSync(stateRoot,{recursive:true});if(!fs.existsSync(this.file))fs.writeFileSync(this.file,'[]\n');}
 list(){try{return JSON.parse(fs.readFileSync(this.file,'utf8'));}catch{return [];}}
 get(id){return this.list().find(x=>x.id===id)||null;}
 register(manifest,{builtin=false}={}){const v=validatePluginManifest(manifest);if(v.state!=='SUCCESS')return {state:'BLOCKED',message:'Plugin manifest validation failed.',errors:v.errors};const sig=verifyPluginSignature(manifest);const integrity=pluginManifestDigest(manifest);const rec={...manifest,manifestDigest:integrity,signatureState:builtin?'BUILTIN_TRUSTED':sig.verified?'SIGNED_VERIFIED':'REVIEW_REQUIRED',availability:'REGISTERED_NOT_EXECUTABLE',executionBoundary:'EXTERNAL_SANDBOX_REQUIRED',registeredAt:new Date().toISOString()};const all=this.list().filter(x=>x.id!==rec.id);all.push(rec);fs.writeFileSync(this.file,JSON.stringify(all,null,2)+'\n');this.audit?.append({type:'plugin.register.v1',pluginId:rec.id,manifestDigest:integrity,signatureState:rec.signatureState});return {state:'SUCCESS',message:'Plugin manifest registered. Execution remains disabled until a sandbox runtime is explicitly configured and verified.',plugin:rec,signature:sig};}
 executionStatus(id){const p=this.get(id);if(!p)return {state:'FAILURE',message:'Plugin not found.'};const sandbox=String(process.env.IUV_PLUGIN_SANDBOX_COMMAND||'').trim();if(!sandbox)return {state:'UNAVAILABLE',message:'Third-party plugin execution requires IUV_PLUGIN_SANDBOX_COMMAND; registration alone is not execution.',plugin:p};if(!['SIGNED_VERIFIED','BUILTIN_TRUSTED'].includes(p.signatureState))return {state:'DENIED',message:'Plugin is not signature-verified/trusted.',plugin:p};return {state:'CONFIGURED',message:'Sandbox command is configured. A per-invocation policy/approval check is still required.',plugin:p,sandbox};}
}
